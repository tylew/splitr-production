// File: src/utils.c
#include "utils.h"  // Include the header file for declaration

// Implement the printDebug function
void printDebug(const char *message) {
    printf("DEBUG: %s\n", message);
}

// Implement the computeSum function
int computeSum(int a, int b) {
    return a + b;
}
